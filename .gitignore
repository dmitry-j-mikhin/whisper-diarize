# окружение
.venv/
__pycache__/
*.pyc
.claude/settings.local.json

# всё про конкретные записи — исходники, расшифровки, имена говорящих, страницы
# сравнения — лежит в records/<имя записи>/ и наружу не выкладывается (см. README)
/records/
backup-*/
